package main

import "fmt"

const (
	A = 1 << iota
	B
	C
	D = iota
)

func main() {
	// parallel.FunctionalParallel()
	// parallel.SyncCondUse()
	//	parallel.Multiple_print()
	fmt.Println(A)
	fmt.Println(B)
	fmt.Println(C)
	fmt.Println(D)
}
